{
	"tasks": [
		{

			"label": "run the makefile",
			"type": "shell",
			"command": "make -C ${workspaceFolder}",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	],
	"version": "2.0.0"
}